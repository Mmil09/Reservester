<% if owner_signed_in? %>
 <%= button_to('Logout', destroy_owner_session_path, :method => :delete, class:"btn btn-primary") %>      
<% else %>
  <%= button_to('Login', new_owner_session_path, class:"btn btn-primary")  %>
<% end %>