<% if owner_signed_in? %>
 
  <%= button_to('My Account', edit_owner_registration_path, method: :get, class:"btn btn-primary") %>
  
<% else %>

  <%= button_to('Register', new_owner_registration_path, class:"btn btn-primary")  %>

<% end %>